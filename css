body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

.log-entry {
  padding: 8px;
  margin: 2px 0;
  border-radius: 4px;
  font-size: 12px;
  line-height: 1.4;
}

.request.pending { border-left: 3px solid #ffa500; }
.request.completed { border-left: 3px solid #4CAF50; }
.request.error { border-left: 3px solid #f44336; }

.event { border-left: 3px solid #2196F3; }

.tab button.active {
  background-color: #2196F3;
  color: white;
}
